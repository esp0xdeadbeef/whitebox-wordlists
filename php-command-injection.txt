file_get_contents(
file(
fopen(
fread(
fgets(
include(
require(
include_once(
require_once(
system(
exec(
passthru
`
popen
proc_open
pcntl_exec
assert
preg_replace
create_function
include
include_once
require
require_once